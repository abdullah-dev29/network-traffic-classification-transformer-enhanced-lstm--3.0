# On Colab, do NOT reinstall tensorflow -- Colab ships a CUDA-matched build
# and reinstalling it breaks GPU support. This file is for reference / local
# (non-training) use only.
tensorflow>=2.15
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
pyarrow>=14.0
matplotlib>=3.7
seaborn>=0.13
joblib>=1.3
